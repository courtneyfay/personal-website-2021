import React from 'react';
import SectionHeader from './SectionHeader';

const Skills = () => {
    return (
        <SectionHeader
            text='skills'
        />
    );
};

export default Skills;
